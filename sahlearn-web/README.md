# sahlearn-web

React + Vite + Tailwind CSS frontend for Sahlearn.

## Setup

```bash
cp .env.example .env
# Fill in values
npm install
npm run dev
```

App runs at `http://localhost:5173`.

## Build

```bash
npm run build
```

Output in `dist/`.
